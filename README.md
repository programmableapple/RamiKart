# RamiMarket

A market application with a backend and frontend.

## Project Structure

- **Backend**: Node.js server and API
- **Frontend**: React application

## Getting Started

1. Navigate to the Backend folder and run `npm install` then `npm start`.
2. Navigate to the Frontend folder and run `npm install` then `npm run dev`.
